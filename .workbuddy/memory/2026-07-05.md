# 2026-07-05 工作日志

## 后端架构重构 (ServiceHub Pro)

### 问题
- 报错 `cannot find the table 'public.users' in the schema.cache`，无法注册账号

### 诊断
- 数据库从未创建 schema，无 SQL 迁移文件
- AuthScreen 直接查询不存在的 `users` 表
- 明文密码存储与比对 (P0 安全漏洞)
- 绕过 Supabase Auth，无 JWT/会话管理
- 无 RLS 策略
- 幻影 REST API (192.168.1.100:3000) 不存在，静默回退 mock 数据

### 交付
1. `supabase/migrations/00001_initial_schema.sql` — 9张表 + 索引 + RLS + 触发器
2. `supabase/migrations/00002_seed_data.sql` — 服务目录种子
3. `src/lib/supabase.ts` — 重构客户端 (AsyncStorage session + schema config)
4. `src/services/auth.service.ts` — Supabase Auth 认证服务
5. `src/services/database.service.ts` — 数据访问层 + Realtime
6. `src/screens/AuthScreen.tsx` — 接入新 auth service
7. `docs/BACKEND_ARCHITECTURE.md` — 架构文档
8. `overview.md` — 概览

## 注册页服务类目选择器改造

### 问题
- 注册页 "Specialty Work Category" 是自由文本 TextInput，用户可以输入任意值
- 需改为只能从平台提供的服务类目中选择

### 修改
1. `src/services/database.service.ts` — 新增 `fetchServiceCategories()` 从 `services` 表查询活跃类目，带硬编码兜底
2. `src/screens/AuthScreen.tsx` — TextInput 替换为可选中 chip 按钮 (`flexWrap` 布局)，组件挂载时 `useEffect` 加载类目列表，含 "All" 选项
3. `src/services/auth.service.ts` — `workCategory` 类型从硬编码联合类型改为 `string`，适配动态类目
4. `supabase/migrations/00001_initial_schema.sql` — `handle_new_user()` 触发器补上 `work_category` 字段写入
5. `src/screens/technician/TechnicianDashboard.tsx` — 过滤器补上缺失的 `beauty` 类目

## Email Confirmation 注册流程优化

### 问题
- Supabase 开启了 email confirmation，用户注册后无法自动登录
- "email rate limit exceeded" 错误用户体验差
- 注册后只有一个 Alert 弹窗，用户不知道下一步该做什么

### 修改
1. `src/services/auth.service.ts` — 新增 `resendConfirmation()` (封装 `supabase.auth.resend`) 和 `refreshSession()` (确认后自动登录)
2. `src/screens/AuthScreen.tsx` — 注册后显示内联 "Check Your Email" 界面（替代 Alert），含：
   - 显示已发送邮件的地址
   - "Resend Confirmation Email" 按钮（60s 冷却倒计时）
   - "I've Confirmed — Sign Me In" 按钮（用户点邮件链接后回来直接登录）
   - "Back to Sign In" 链接
   - 优雅处理 rate limit / already registered 等错误

## 技师端 UI 重设计

### 任务
- 用户要求技师端页面全部重新设计 UI，提供更好的技师工作 UX
- 保持与客户端相似的设计语言，英文界面
- 客户端页面不改动

### 交付
- `.workbuddy/designs/technician-ui-redesign.html` — 高保真交互式 HTML 原型
- 包含 7 个屏幕：Dashboard(Jobs/Schedule/Profile)、Job Details、Active Service、Service Completion、Success State
- 设计改进点：
  - Jobs tab: 优先级指示条 (高/中/低)、趋势统计卡片、圆角筛选 pill
  - Schedule tab: 时间线视图替代简单列表、日期点指示有任务、时段开关
  - Profile tab: 渐变 hero 卡片、三栏统计、技能标签、分区信息卡
  - Job Details: 预估收益徽章、地图预览、清单进度条
  - Active Service: 大号计时器卡片带状态指示、清单进度条
  - Completion: 支付明细高亮卡、前后照片上传槽、清单完成度
  - Success: 大号收益展示、成功动画
- 设计令牌：主色 #003d9b、背景 #f0f3f9、圆角 12-20px、卡片白底+细边框

### React Native 实现
- 用户确认 HTML 原型后，将全部设计落地为 React Native 代码
- 修改文件：
  1. `src/screens/technician/TechnicianDashboard.tsx` — 全部重写 3 个 tab (Jobs/Schedule/Profile)，使用 LinearGradient hero、时间线视图、优先级条、趋势统计、技能标签
  2. `src/screens/technician/JobDetails.tsx` — 重写：渐变收益徽章、客户联系按钮、地图预览占位、清单进度条
  3. `src/screens/technician/ActiveService.tsx` — 重写：渐变计时器卡片+状态 pill、清单进度条、高亮运行总计条
  4. `src/screens/technician/ServiceCompletion.tsx` — 重写：支付明细高亮卡、前后照片上传槽、成功页大号收益展示
  5. `src/navigation/AppNavigator.tsx` — 新增 PillTabBar 自定义组件，浮动 pill 样式底部导航
- 设计令牌统一：C 对象集中管理颜色，R 对象管理圆角
- 保留全部业务逻辑：API 调用、状态管理、导航参数传递
- TypeScript 编译通过（技师端 0 错误）

### ⚠️ 回滚：用户要求撤销全部技师端 UI 改动
- 用户反馈"older version is better"，要求撤销所有改动
- 项目无 git，从 `Downloads/ServiceHub-App-main.zip` 原始压缩包中提取 5 个文件并覆盖恢复：
  1. `src/screens/technician/TechnicianDashboard.tsx`
  2. `src/screens/technician/JobDetails.tsx`
  3. `src/screens/technician/ActiveService.tsx`
  4. `src/screens/technician/ServiceCompletion.tsx`
  5. `src/navigation/AppNavigator.tsx`
- 同时删除了 `.workbuddy/designs/technician-ui-redesign.html` 设计原型
- 所有技师端代码已恢复为原始版本，客户端未受影响

## 动态岛遮挡修复 + 性能优化 + 错误修复

### 动态岛/安全距离修复
- 问题：所有屏幕使用硬编码 `paddingTop: 48`，动态岛/状态栏遮挡内容
- 修复：App.tsx 添加 SafeAreaProvider，全部 12 个屏幕用 SafeAreaView 替换硬编码 paddingTop
- 涉及文件：App.tsx, AppNavigator.tsx, AuthScreen, SelectRole, CustomerHome, TechnicianDashboard, ServiceDetails, ScheduleDetails, Checkout, Tracking, SupportChat, JobDetails, ActiveService, ServiceCompletion

### 性能优化
- AppNavigator: 用 useMemo/useCallback 优化 AppContext，避免不必要重渲染
- CustomerHome/TechnicianDashboard: 用 expo-image 替换 Image，添加 React.memo

### 错误修复 (TypeScript 编译错误)
- CustomerHome.tsx: 修复重复 "Profile" 文本行；修复 `memo()` 未闭合（缺少 `)`）；修复 `startBooking` 函数缺少 `technician` 可选参数
- TechnicianDashboard.tsx: 修复 `memo()` 未闭合（缺少 `)`）
- AppNavigator.tsx: AppContext 类型从 `(jobs: Job[]) => void` 改为 `React.Dispatch<React.SetStateAction<Job[]>>`，支持 Checkout.tsx 中的函数式 `setJobs` 调用
- ActiveService.tsx: 修复 `setSeconds` 回调参数隐式 any 类型
